"use client";

import AuthPanel from "@/components/public/landing/auth-panel";

const Update = () => {
  return (
    <div className="min-h-screen flex">
      <AuthPanel />
      <div className="flex-1 flex justify-center items-center w-full relative">update password page</div>
    </div>
  );
};

export default Update;
